import arrayCalc from '../calculators/Array'

const ExtraPanel = ({arrayResult, width = 0, type, angle=0}) => {
	const {
		arrayLayout,
		spareSpaceSides,
		spareSpaceTopBott,
		panel,
		distance,
		orien,
	} = arrayResult || 0

	
	return <div></div>
		
	
}

export default ExtraPanel
